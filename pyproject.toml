[project]
dependencies = [
  "click>=8.0",
  "requests>=2.32.5",
  "sqlalchemy>=2.0",
  "tenacity>=9.0",
]
description = "Add your description here"
name = "playhub-scraper"
readme = "README.md"
requires-python = ">=3.9"
version = "0.1.0"

[project.scripts]
playhub = "main:cli"
